{
  "name": "zynapse-chat-app",
  "version": "1.0.0",
  "description": "A macOS-style chat application",
  "scripts": {
    "start": "live-server --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
